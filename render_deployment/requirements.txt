# Explicitly use CPU-only PyTorch wheels to stay under Render's 512MB RAM constraint
--extra-index-url https://download.pytorch.org/whl/cpu

# Core Dependencies
torch==2.11.0+cpu
torchaudio==2.11.0+cpu

# Web server dependencies
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
soundfile>=0.12.1
pesq
pystoi
scipy
